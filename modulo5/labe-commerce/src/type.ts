export type CadastroUser = {
    id: string,
    name: string,
    email: string,
    password: string
};

export type CadastroProduct = {
    id: string,
    name: string,
    price: number,
    image_url: string
};